% vim: tw=78 ai spell spelllang=en

\documentclass[10pt]{llncs}
\usepackage{color}
\usepackage{url}
\usepackage{graphicx}
\usepackage{listings}
\usepackage{multirow}
\usepackage{xspace}

\newcommand\Color[2]{\textcolor{#1}{#2}}
\newcommand{\nb}[1]{\marginpar{\scriptsize #1}}
\renewcommand\note[1]{\nb{\Color{blue}{#1}}}
\newcommand\todo[1]{\nb{\Color{red}{#1}}}

\lstset{escapeinside={(*@}{@*)}}

\begin{document}

\title{Finding Bugs by Automata Using Symbolic Execution}
\author{Jan~Strej\v{c}ek, Ji\v{r}\'\i~Slab\'y and Marek~Trt\'{\i}k}
\institute{Faculty of Informatics, Masaryk University, Brno, Czech Republic
\email{\{strejcek,slaby,trtik\}@fi.muni.cz}}

\maketitle

\begin{abstract}
\end{abstract}

\section{Introduction}
\label{sec:introduction} \todo{H}

SE King~\cite{se}

\textsc{Metal}~\cite{metal}
\textsc{Stanse}~\cite{stanse} \note{este nevyslo takze asi nic}
\textsc{Klee}~\cite{klee}

\section{Instrumentation}
\label{sec:instr}

We want to find a similar set of errors to those found by tools like
Metal~\cite{metal}. These tools use abstract interpretation as an algorithm
and automata for program behavior description. To be able to track a state of
automata using the symbolic execution, the code has to be instrumented before
the execution itself. However, the code instrumented is different to the
behavior performed by the automata in abstract interpretation on the same
code.

Abstract interpretation has to consider a set of states by each program
location and a transform function over these sets for the locations. It
is due to exploring all paths of the program at once. For the symbolic
execution it is sufficient to remember the actual state of the program path
explored. The other paths are explored separately in a different run.

To present the difference of the instrumentation before the symbolic execution
and computing abstract interpretation automata states,
Fig.~\ref{fig:instrumentation} is attached. Consider a function \texttt{foo}
where a lock A is used. Further there is a \texttt{compute} function.  Assume
that it changes the program state, especially the A state, in no way.

Abstract interpretation, while computing a fix-point, would transform states
at lines~\ref{line:lock} and~\ref{line:unlock} according to an automaton
describing locking. An example of such an automaton is depicted in
Fig.~\ref{fig:automaton}.  In that specific case, line~\ref{line:lock}
transforms unlocked state into locked one and locked state into an erroneous
one. The latter is eventually reported by the tool as an error to the user.
Similarly for line~\ref{line:unlock}.

Thanks to the simple run being evaluated at once, the lock may be only in one
state at a time. Hence we introduce an integer for each lock used. The integer
then corresponds exactly to the automaton state. I.e. all function calls to
\texttt{lock} and \texttt{unlock} are replaced by assignments to this integer
by one or zero respectively. Now it reminds to describe the way of error state
checking.

Thanks to the symbolic executor we use (cf. Section~\ref{sec:impl}), a
so-called \texttt{assert} function may be used to provoke the executor to
report a failure. The function (or macro more precisely) is used by developers
for debugging purposes. The macro takes exactly one argument. When it
evaluates to true, an error is reported. Both when a program containing the
facility is run and when executed by the symbolic executor we use.

Thus our tool inserts an \texttt{assert} before each assignment. There the
assertion is that the integer is zero when the code is about to take a lock
and one in the opposite case. This can be seen in
Fig.~\ref{fig:instrumentation}, column three.

\begin{figure}[tb]
\begin{center}
\begin{tabular}{|l l|l|l|} \hline
& \textbf{Original code} & \textbf{AI} & \textbf{Instrumentation for SE} \\ \hline
\begin{lstlisting}{language=C}
1
2
3
4
5
\end{lstlisting} &
\begin{lstlisting}{language=C}
foo() {
  lock(A);(*@ \label{line:lock} @*)
  compute();
  unlock(A);(*@ \label{line:unlock} @*)
}
\end{lstlisting} &
\begin{lstlisting}[language=C]
foo() {
  state_change
  no_state_change
  state_change
}
\end{lstlisting} &
\begin{lstlisting}[language=C]
foo() {
  assert(A = 0); A := 1;
  compute();
  assert(A = 1); A := 0;
}
\end{lstlisting} \\ \hline
\end{tabular}
\end{center}
\caption{Code a) we want to check; b) that is used for abstract interpretation
and c) instrumented for use with symbolic execution}
\label{fig:instrumentation}
\end{figure}

\begin{figure}[htb]
\begin{center}
\includegraphics[width=3cm]{lock_unlock}
\end{center}
\caption{An automaton describing locking behavior}
\label{fig:automaton}
\end{figure}

\paragraph{Points-To Analysis} To be precise in decision about pointers in our
tool, we implemented Andersen points-to analysis~\cite{andersen}. Primarily,
it helps us in slicing to decide if two pointers are the same or not. Further
it determines which functions are called by indirect function calls, which
locks point to the same allocation during instrumentation phase even though
the locks have a different name and so on.

\section{Slicing}
\label{sec:slicing} \todo{M}

\section{Implementation}
\label{sec:impl}
We decided to verify the functionality on the C code. The main reason is that
many projects with open-source license (and source code available) are written
in C. Hence there is a fair codebase to test.

The tool is not implemented from scratch. It is written as a module for the
LLVM framework~\cite{llvm}. This provides us with a C parser (and compiler)
for free and a documented application programming interface. Also for LLVM
a symbolic executor exists already. It is called \textsc{Klee}.

Before we can really run the executor, several steps have to be performed
first. Initially, the tool instruments the code on the C level. Each locking
function is superseded by an \textsc{assert} and assignment to an appropriate
integer to reflect the automaton state. This task is performed by our first
LLVM module called \texttt{Prepare}.

The instrumented C code is then taken by the tool and turned into LLVM code.
For this purpose a compiler called \textsc{clang}\footnote{\textsc{clang} is a
part of the LLVM framework.} is used.

Further the LLVM bytecode is sliced by an LLVM inter-procedural slicer to
eliminate unneeded (irrelevant) code. This is done by a separate LLVM module
\texttt{Slicer}.

Given the sliced input, we parse the LLVM code with \texttt{Kleerer} LLVM
module. This pass creates a separate file with LLVM bytecode for each function
in the file. It also adds \texttt{main} function which is where the analysis
start -- in each of the files it calls a different function.

\begin{figure}[htb]
\begin{center}
\textcolor{red}{Do we want this figure (picture needed)?} \\
C $\rightarrow$ Instrumentation (Prepare) $\rightarrow$ LLVM compiler (clang) $\rightarrow$ slicing (Slicer) $\rightarrow$
main creator (Kleerer) $\rightarrow$ sym. exe. (Klee)
\end{center}
\caption{Phases of our tool}
\label{fig:phases}
\end{figure}

\subsection{Experiments}
\label{sec:experiments}

The implementation described above was evaluated on a real code too. The
evaluation was performed on the Linux kernel. It is a large and well tested
piece of software. Also it is generally of high interest to be bug-free.
Otherwise systems could be indeed compromised rather easily.

All tests performed were run on a standard office machine. On a hardware
level, its configuration is a dual-core processor, 6\,GiB of memory.
Operating system installed is Linux. \textsc{Klee} is run on the instrumented
and sliced LLVM code. We specify \textsc{Klee} parameters to add an upper
bound on solver and overall time. The former is set to 3 seconds, the latter
to 30. We tried to increase these times with no real effect.

We do not optimize option for \textsc{Klee} because it causes \textsc{Klee} to
crash under some circumstances. The optimization might help the analysis,
hence the crashes are yet to be investigated.

At this early stage of the tool, the evaluation is not performed on the whole
kernel. It is because it still does not scale that much. We plan to extend
the tool to be capable of handling whole kernel. We limited our testing to a
set of files. We aimed the technique at the functions where XXX\todo{what to
write here} reported a trace. Be it a real error or false positive. We
consulted the files with upstream developers to sort out which are in each
category.

This helps us to sort the results provided by our tool. The output is the tool
either:
\begin{itemize}
\item confirms a real error -- reports an error at the same location the
abstract interpretation tool did or
\item confirms a false positive -- does not report anything at that location
or
\item does not finish computation -- due to various errors.
\end{itemize}

The last category stems from two sources. First, the tool hits a limit of
symbolic execution and times out. This happens especially when locks are
dependent upon loops. Then the loops are not sliced away and symbolic
execution does not finish in the bounded time.

Second, memory errors occur. This is due to our functions input handling. For
each parameter of a function analyzed, a symbolic memory is allocated. Size is
determined as the parameter size. When the parameter is a pointer, the size is
multiplied by 4000. So for example 4 bytes for integer, and 16000 for integer
pointer. Further, for the pointer case, we pass a pointer to the middle of the
array (functions might dereference memory at negative index). The idea behind
is explained in~\cite{ucklee}.

Even if we use this feature, function may touch a memory out-of-bounds this
array. Typically when there is a pointer to a structure passed to a function
and when the structure contains another pointer. It is uninitialized. We plan
to implement the same on-demand handling \textsc{UcKlee}~\cite{ucklee} does.

For files from each of the three categories we present examples where we are
sure about the results. I.e. upstream developers confirmed they are either
error or false positive and we are able with our limited knowledge to reason
about the files. They are presented in Table~\ref{tab:results}. It contains
running time of each step performed on the files.

\begin{table}[t]
\begin{tabular}{|l|l|l|l|l|l|} \hline
\multirow{2}{*}{\textbf{File and Function}} &
  \multicolumn{5}{|c|}{\textbf{Running Time}} \\
 & \textbf{Compilation} & \textbf{Instrumentation$^{\dagger}$} &
   \textbf{Slicing} & \textbf{Symbolic Execution} & \textbf{Total} \\ \hline
file1.c foo & & & & & \\ \hline
\end{tabular}
\caption{Technique run on several files. $\dagger$ Including the points-to
analysis time.}
\label{tab:results}
\end{table}

\section{Related Work}
\label{sec:related} \todo{M}

\section{Future Work}
\label{sec:future} \todo{H}

\textsc{UcKlee}\cite{ucklee}

\section{Conclusion}
\label{sec:conclusion} \todo{H}

\paragraph{Acknowledgements}
\label{sec:acknowledgements}

All authors are supported by the research centre Institute for Theoretical
Computer Science (ITI), project No. 1M0545.

\todo{ITI skoncilo, pridat DG}

\bibliographystyle{plain}
\bibliography{sse}

\end{document}
