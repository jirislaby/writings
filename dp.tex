% vim: textwidth=78 ai spell spelllang=en

\documentclass[draft]{fithesis}

%\usepackage{cmap}
\usepackage[utf8x]{inputenc}
\usepackage[english]{babel}
\usepackage[IL2]{fontenc}
\usepackage{hyperref}
\usepackage{graphicx}
\usepackage[final]{listings}

\thesislang{en}

\thesistitle{Rapid data transfers on COMBO platform}
\thesissubtitle{Master's thesis}
\thesisstudent{Jiří Slabý}
\thesisadvisor{Ing. Pavel Čeleda, Ph.D.}

\thesiswoman{false}
\thesisfaculty{fi}
\thesisyear{spring 2008}

\newcommand{\lbr}{\emph{Liberouter}}
\newcommand{\com}{\emph{COMBO6}}
\newcommand{\comx}{\emph{COMBO6x}}
\newcommand{\fpga}{\emph{FPGA}}
\newcommand{\dma}{\emph{DMA}}
\newcommand{\sze}{\emph{szedata2}}
\newcommand{\ncope}{\emph{NetCOPE}}

\lstset{tabsize=2,numbers=left,showstringspaces=false,breaklines=true,
	breakatwhitespace=true,escapeinside={(*@}{@*)},frame=l}

\begin{document}
\FrontMatter
\ThesisTitlePage
\begin{ThesisDeclaration}
  \DeclarationText

  \begin{flushright}
  In Brno, \today \\
  Jiří Slabý
  \end{flushright}
  \AdvisorName
\end{ThesisDeclaration}
\begin{ThesisThanks}
  I want to thank my thesis advisor Pavel Čeleda. And least but not the last
  I would like to thank here my parents for their patience too.
\end{ThesisThanks}
\begin{ThesisAbstract}
  In this thesis we are concerned in fast transfers from hardware to computer
  programs and vice versa. Above all we specialize to coherent direct
  memory access transfers from/to hardware through ring buffers. Also we
  discuss other transfer methods implemented in other projects and consider
  using them here. Finally we implement the method and test it with all
  available tools, describe the testing process and compare with already
  working products.
  
\end{ThesisAbstract}
\begin{ThesisKeyWords}
  Linux, Driver, ring buffer, mmap, DMA, COMBO6x, NetCOPE, Liberouter
\end{ThesisKeyWords}

\MainMatter
\tableofcontents

\chapter{Introduction}
  To even boot a computer, it's necessary to proceed many transfers inside the
  hardware among its components. It's a basic function which must work,
  however there are several methods of how to achieve that. Especially
  the fast ones are very important in computer science which results in higher
  overall throughput of the system itself and also in faster communication with
  other ones.

%  There are many computer technology areas, where fast transfers are very
%  important. Beside the computer-inside transfers among CPU, disks,
%  DVD writers and main memory, we must take in account also the outside ones,
%  such as moving huge data amounts from/to external disks, monitors or data
%  cards in cellphones for example; all over buses with specific
%  data formats. Outstanding speed is important especially in the area of
%  network communication -- as an example backbone network routers or
%  monitoring probes are very dependent on how fast data transfers can be
%  achieved in the end.

%  Nowadays the equipment of operating systems can do fast network transfers
%  through well-known interfaces, but like in other areas, even here is a
%  space for improvements, for methods which are not so common, but might
%  provide yet another significant speedup.

  As an answer to permanent higher requirements in the networking, Liberouter
  has been established as a project supported by Czech academic network CESNET
  and European Internet project 6NET. The original intention was to develop a
  router
  for high traffic networks. After some time, this idea was augmented to other
  network usages such as network filtering and monitoring. All these functions
  run on \com\ family PCI cards (on figure \ref{fig.combo6} for illustration).

  \begin{figure}[tb]
    \begin{center}
      \includegraphics[width=8cm]{images/combo6}
      \caption{\comx\ card}
      \label{fig.combo6}
    \end{center}
  \end{figure}

  This thesis is based on one of many researches that have been already done
  under Liberouter -- \ncope\ -- which we describe in chapter
  \ref{chap.netcope}.
  In short, it's hardware platform trying to achieve universality along with
  speed and it's fully programmable. Software running on them can do fast
  transfers if it is instructed to. For this purpose are usually used drivers.
  Despite some are already prepared, a new \ncope\ software was developed and
  it needs a new driver.

  This thesis describes the evolution process of such driver from its
  requirements, considering variants to be as fast as possible but still be
  compliant with hardware specification \cite{netcope}, especially
  direct memory access (see \ref{chap.dma}) in combination with ring buffers
  (see \ref{chap.netcope}).

  After requirements
  settle down, we model the specification with help of UML and get towards
  final code implementation in GNU C language (see \cite{GNUC}). As much as
  possible code parts (all corner cases at best) need testing, we figure out
  which testing techniques might be used for drivers and finally comparing
  with older drivers and card softwares will be done.

%  For
%  instance nowadays mainly used technique of \emph{direct memory access} in
%  cooperation with ring buffer data structure and memory mapping (so-called
%  \emph{mmap}) might move big chunks of data without even processor
%  assistance from network adapter towards application in userspace and vice
%  versa without any real data copying. This results in short transaction
%  times.
  
%\chapter{Basic terms}
  \paragraph{Linux}
  In the early stages, we decided to create a driver implementation only for
  Linux operating system. It is an operating system based on kernel which was
  established in the
  early nineties of the 20th century by Linus Torvalds. There is over 3
  hundreds variants called distributions shipped by either communities or
  software giants.
  
  Linux kernel in its version 2.6 has no stable application programming
  interface (API) inside the kernel, but still the API is well-evolved and
  easy to use, because the
  development is ridden and checked by a Linux kernel community. We will
  see consequences of such development method in detailed description of some
  subsystems later and in compatible layer implementation too.

  Major part of Linux kernel is licensed under GPL version 2 \cite{gplv2}.
  Chosen license which will be the implementation distributed under is GPL
  version 2 like most other drivers.

\chapter{Hardware basics}
  Before we can start coding, many hardware questions must be answered to know
  under which circumstances we can achieve concrete goals and what constraints
  kernel driver implementation has. Understanding hardware-software
  communication will guide us towards complete implementation.
  
  After each section, we consult what exactly does that particular part
  means for the final code.

  \section{Devices interconnection}
  \label{chap.inter}
  The first thing to explain is how data flows from devices to processor and
  main memory and particularly which types of buses are on the route from a
  CPU to a device. Overall view may be seen on figure \ref{fig.dev_inter}.

  \begin{figure}[htb]
    \begin{center}
      \includegraphics[width=9cm]{images/data_flow}
      \caption{Devices interconnection}
      \label{fig.dev_inter}
    \end{center}
  \end{figure}

  \subsection{PCI}
  \label{chap.pci}
  We are concerned especially in \emph{peripheral component interconnect}
  (PCI) devices, because both \com\ card families are PCI based. Each such
  device is physically connected to any PCI bus via PCI slot.
  PCI bus is attached to \emph{southbridge}, it communicates
  with \emph{northbridge} over internal bus and it's finally wired
  with processor via \emph{front-side bus} (FSB) and with memory. Nowadays
  it's still more common, that all southbridge, northbridge and memory
  controller are implemented as proper PCI devices directly connected to the
  main PCI bus.
  
  Older PCI buses have a \emph{control bus} supposed to send controlling
  signals and synchronize objects which are connected to it and multiplexed
  \emph{address} and \emph{data buses}. Device starts transmission by sending
  address, then follow data
  on the same wires. On the other hand, PCI express uses packets for
  communication over serial physical layer, it further must contain hubs and
  switches for packet transmission. Data and addresses (might be both 32 and
  64-bit) are part of packets, each request and reply is a packet here. 

  Legacy PCI has 32-bit wide address/data bus. This shortcoming affects
  sending 64-bit numbers over the
  bus to older \com\ cards as we will see in section \ref{chap.hwsetup}.
  Further detailed PCI information are in \cite{pcispec} (PCI, PCI-X) and
  \cite{pciespec} (PCI express).

  \subsubsection{Configuration space}
  Every modern bus supports a mechanism to autoconfigure devices connected
  to it. Also each PCI device has its configuration space, where all the needed
  information are stored or may be set to. This space contains information
  about who developed the device and which model it is. These data are useful
  especially in driver area, where the drivers want to bind only to specific
  subset of devices.

  In our scope of interest are device, vendor and their subsystem variant
  IDs contained in this space, because these are mostly only identifiers
  designating one concrete device family. It is helpful in userspace scripts
  to create persistently named nodes in chapter \ref{chap.udev}.

  \subsubsection{Linux PCI support}
  PCI is well supported in Linux since it became a standard. Concrete API is
  in a scope of chapter~12 of \cite{ldd}. In short, driver's hook is called
  whenever subsystem finds suitable PCI device, initialization is done by
  functions with pci\_ prefix, especially device must be enabled and spaces
  remapped to be usable. Then the device is ready to work. Deinitialization
  and device switch off is done in driver's remove hook.

  Transfers are done by processor by standard read and writes over the
  remapped space, unit by unit. On 32-bit unit is up to 4 bytes, on 64-bit up
  to 8 bytes. These transfers are not too fast and need processor attention.

  \section{DMA and Bus Mastering}
  \label{chap.dma}
  \emph{Direct memory access} known as DMA is a method widely used to
  speed up transfers in computer engineering. It's based on technique called
  \emph{bus mastering}. A device which wants to move data from/to memory
  becomes a bus master and no other device or even processor can't touch
  address and data bus. While being a master, it sets address on an address
  bus and sends or latches data on a data bus. When there are more than one
  data phases after address phase on multiplexed bus, the transfer is done in
  so-called \emph{burst mode}.
  
  On older machines, there was only one device called DMA controller (DMAC),
  which could become a master. Nowadays each PCI device (see also Section
  \ref{chap.pci}) may become a master, so that it can access memory directly
  by its own DMAC\footnote{Note that there is no common PCI DMAC, so if the
  device doesn't have one, it doesn't support DMA either.}. DMAC count is not
  bounded to only one per device as we will see implementation of \com\ 
  hardware in section \ref{chap.netcope}. Unit which takes care of honest bus
  accesses by more than one DMACs is called \emph{Arbiter}.

  \subsection{PCI DMA interface}
  To become a master on legacy PCI bus, the device asks arbiter for a band and
  after it gets start signal on control bus, it starts sending. First,
  address is sent, then data part.
  If there is more data to send, burst mode is used until arbiter emits stop
  signal. After that signal, the device must finish the transfer of near few
  bytes in the cache line\footnote{its size is configurable through config
  space} and acknowledge stopping. Next device gets start signal from arbiter
  at this moment and starts data transfer. The ancestral device may ask for a
  band again unless all data have not been sent. This method is implemented in
  PCI and PCI-X versions of \com\ cards.

  PCI express is a bit different, it asks the other device to get some amount
  of data in request packet and the device fills reply packet with the data.
  Sending is similar, the source device fills the data
  directly in the request and gets back completion packet. This is obviously
  implemented in PCI express based \com.

  The transfer is theoretically limited by the other device (including main
  memory) boundary\footnote{it is 4096 bytes on main memory when extensions
  disabled}. It must not
  exceed this limit, the transfer is interrupted by the other side otherwise.
  Transfers of 1\,KiB per one transfer are not uncommon in \com\ case.

  \subsection{Linux DMA interface}
  Linux has three kinds of DMA APIs. ISA/LPC, PCI and generic. ISA/LPC is used
  only for old 8237 chips located on old ISA buses and on todays LPC buses,
  neither one is in our scope of interest.

  \paragraph{PCI interface} supports coherent (also called consistent) and
  non-coherent DMA mappings.
  We always must decide which one we should use. Non-coherent mappings are
  often mapped only for a transfer and then unmapped and underlying buffer
  freed. This is method widely used for mapping socket buffers which are
  threw away after
  successful transmission. We will probably use this interface for exactly
  this purpose out of scope of this thesis, see chapter \ref{chap.future}.

  The other group of coherent are mappings usually
  mapped at the driver load or device start and unmapped and freed even after
  device stop. Its domain are DMA ring buffers, firmwares used by some
  devices and executed from main memory etc. This mapping hence perfectly fits
  our needs and will use it, see \ref{chap.netcope}.

  \paragraph{Generic interface} It is actually an abstraction of PCI interface.
  It's not bounded to any PCI device, it may be used for non-PCI devices,
  however it obviously doesn't support all the PCI features.

  \section{Memory management}
  \label{chap.mm}
  This chapter is a short introduction into memory management as used not
  only in Linux. Memory management in details on Intel processors is described
  in \cite{ia1} and also in \cite{gorman} and \cite{ulk}, chapter~2.

  \subsection{Physical memory}
  In the usual meaning we call main \emph{random access memory} as a physical
  memory and we usually\footnote{It's possible not to have this kind of memory
  by a processor in special computers like \emph{NUMA} (non-uniform memory
  access).} have one or more of them plugged in the motherboard. These can be
  addressed by several methods; Linux implements \emph{Flat mode}, where
  each byte of memory is simply addressed by \emph{linear address}. First
  memory byte has address 0, on 32-bit without any extensions the last
  possible byte (if there is that much physical memory) has address
  $2^{32}-1$.

  \subsection{Virtual memory}
  With only physical memory addressing problems arose early. Applications
  needed
  continuous big chunks of memory, but it was impossible to find such long
  block after a while system was running. It was caused by process called
  (external) fragmentation. Other tasks held memory blocks and there wasn't
  enough space in between them to return. See figure \ref{fig.fragmentation}
  for illustration.
  
  Task 1 holds two memory blocks of size 2 units. Task 2
  allocated one block three units wide. Now, if any of the two wants to
  allocate block of size 4, allocation will fail, although there is
  enough free space in sum.

  \begin{figure}[htb]
    \begin{center}
      \includegraphics[width=7cm]{images/fragmentation}
      \caption{Memory fragmentation illustration}
      \label{fig.fragmentation}
    \end{center}
  \end{figure}

  Many algorithms were introduced to avoid this behavior, some of them are
  described in \cite{knuth_art} including buddy system used in Linux
  kernel for in-kernel allocations.
  
  \subsubsection{Paging}
  To solve the situation with external fragmentation, method named
  \emph{paging} was introduced. Linear address space is divided into pages
  of fixed size\footnote{4096 bytes on x86 with page size extension
  disabled} that might be mapped to physical memory or disks as swapped pages.

  CPU accesses physical memory by virtual addresses with help of \emph{memory
  management unit} (MMU) which does transparent translation to physical
  address. Some modern
  mainboards add another unit called IOMMU between device and main memory
  path, so that devices address by logical address too. It allows creating a
  virtual space even for devices, but it's not widely spread so far
  so \com\ card developers must not take it for granted.

%  Virtual address itself is usually divided into few parts and each of them is
%  offset in some page table. On 32-bit x86 with extensions disabled, 2 such
%  tables exist and each table contains entries/pointers to next table or
%  to physical page in the last table respectively.
%  If the entry in either
%  table is marked as non-present, \emph{page fault} (exception handled by
%  operating system) is thrown. OS must either put (map) the requested page in
%  or kill the program which references illegal address.
%  On figure \ref{fig.vaddr} is shown, how is logical address split. CR3 is
%  register in x86 processors containing location of global page directory
%  depicted on the left side.
%  \begin{figure}[htb]
%    \begin{center}
%      \includegraphics[width=9cm]{images/vaddr}
%      \caption{Virtual address format}
%      \label{fig.vaddr}
%    \end{center}
%  \end{figure}

  This schema allows us to allocate huge contiguous memory blocks without a
  need of having contiguous physical memory. In fact there is no need to have
  as much physical memory as requested. Some memory may be, as written above,
  backed by a page stored on disk (swapped out) or even have no "physical"
  page mapped to it thanks to demand paging.

  On 32-bit, we are able to use up to 3 gibibytes\footnote{SI unit for
  $2^{30}$} of virtual space per process. This limit comes from 1:3 of
  kernel to user address space split -- the remaining addressable
  1\,GiB is used as kernelspace. On 64-bit, 48-bit virtual addresses are
  supported by most current processors, up to $2^{47}$ may be used in
  userspace, kernel is mapped in top $2^{47}$ bytes due to 64-bit sign
  extension (see \cite{ia3a} for reference) and the space between them is an
  unused memory hole, which might be addressed even after newer processor
  family will be available.

  Consequences of all these constraints are considered later in section
  \ref{chap.netcope}, because we cannot, as we mentioned earlier allocate
  too much contiguous physical space and virtual memory is not unbounded
  too, especially on 32-bit machines.

  \paragraph{Linux memory interface} To obtain a buffer smaller than a page
  is used \emph{kmalloc} and \emph{kzalloc} returning a zeroed space. It's
  calls of slab allocator (see \cite{gorman}, chapter~8). Freeing is provided
  by \emph{kfree}.
  
  For larger physically contiguous buffers (consequent pages)
  \emph{get\_free\_page} family should be used -- variants for zeroed page(s),
  for single page, for functions which returns \emph{struct page} pointer
  instead of logical address. Freeing is done by \emph{free\_page}. This
  page allocations might be theoretically used for DMA, its physical addresses
  could be gained by \emph{page\_to\_phys} (\emph{struct page}) or
  \emph{virt\_to\_phys} (logical address), however this is not clean, because
  the returned address needn't be device addressable. We should call
  \emph{dma\_alloc\_coherent} to get an addressable page. It gets
  \emph{struct device} as one of parameters where DMA capabilities are
  written down and so it knows which addresses are OK to return. Possibly
  wrapper \emph{pci\_alloc\_consistent} might be used to pass \emph{struct
  pci\_dev} directly. \emph{dma\_free\_coherent} and
  \emph{pci\_free\_consistent} serves as cleanups.

  \emph{vmalloc} is for in-kernel virtual contiguous memory allocations.
  Another wrappers like \emph{vmalloc\_32}, \emph{vmalloc\_user} and
  \emph{vmalloc\_32\_user} exist for allocating buffers addressable by 32-bit
  addresses and for those intended to use in userspace as of 2.6.18 --
  underlying pages are marked as user and zeroed not to leak any information.
  \emph{vfree} frees all these allocations.

  For the convenience of \com\ PCI devices, we use
  \emph{pci\_alloc\_consistent} and \emph{pci\_free\_consistent} for DMA
  allocations, \emph{vmalloc\_32\_user} for buffer to be mapped to userspace
  and obviously \emph{kmalloc} for driver state structures.

  \subsection{Memory map}
  As we are able to set (and unset) present bit in an entry of each table
  MMU traverses and an exception is thrown when the bit is not set when the
  particular table entry is accessed, we can hook a page fault handler to this
  path which will install missing pages.

  This can be used for \emph{memory mapping} (MMAP). We register a page fault
  function by OS interface and when user application maps some space and
  accesses it, we may set up there any pages we want. According to exact
  address which was the root cause of the fault, we compute a number of page
  which should be in that particular memory place and install it there.
  Application sees this as a transparent process and it accesses the data from
  the installed page since that moment.

  \paragraph{Linux mmap interface}
  Many incompatible interfaces were introduced and removed meanwhile Linux
  2.6 development process. First versions support remapping of some kind of
  memory only by implementing real
  fault handler called \emph{nopage}. This hook is called for every page fault
  and must install the requested page (offset is passed in as a parameter) or
  fail to program be killed. \emph{nopage} hook was changed to \emph{fault}
  with slightly modified prototype in 2.6.23.

  Not to implement \emph{fault} or \emph{nopage} handler for each mapped
  space, helpers exist. For remapping I/O space, \emph{io\_remap\_page\_range}
  was used in some variants, because it changed prototype time by time, then
  \emph{io\_remap\_pfn\_range} was implemented and it's still in use.
  While for remapping kernel memory obtained by page allocation mechanism
  (i.e. memory which is physically contiguous),
  \emph{remap\_pfn\_range} may be used, a helper called
  \emph{remap\_vmalloc\_range} for virtual memory exists even since 2.6.18 and
  it remaps only \emph{vmalloc\_*user} buffers. We'll use all these mentioned
  interfaces, depending on which kernel we compile for, to be backward 
  compatible and to have all the functionality (and simpleness for later
  kernels).

  \subsubsection{Memory mapped I/O}
  Memory map is sometimes inappropriately confused with \emph{memory mapped
  I/O} (MMIO). We are able to access device memory space in kernel by virtual
  address by MMIO. It's achieved by mapping I/O space into kernelspace.
  For example \fpga\ memory space of a \com\ card may be referred
  by a virtual address after being mapped this way.

  \paragraph{MMIO interface} Memory mapped I/O is done by invoking
  \emph{ioremap} or \emph{pci\_iomap} for PCI devices. Then, for the former,
  \emph{read} and \emph{write} are used for device space access. The latter
  interface is
  slower, because it calls \emph{ioremap} and all consequent read and writes
  (functions \emph{ioread}, \emph{iowrite}) are done through calling back
  \emph{read} and \emph{write} or \emph{in} and \emph{out} depending on what
  kind is the underlying mapping of. In our case we know, it's mapped memory,
  so that we use \emph{ioremap}/\emph{iounmap} and \emph{read} and
  \emph{write} functions.

  We refer to \cite{ldd}, chapter~9 and 15 for further reading about accessing
  device I/O and memory mapping.

  \section{Character device}
  While we are able to pass data from device to memory available only to kernel
  via DMA so far, we still need to put the data to userspace application
  somehow. Several ways how to do it exist in Linux.
  Most of them are based on moving data through special
  files\footnote{most of communication channels are files, even socket
  creating system call returns a file descriptor passed to standard read and
  write functions} such as sockets, block and character devices.

  The basic and simplest idea is to  \textbf{copy} the data by processor
  from a kernel buffer to an user one passed via read system call through
  any instance of a file.

%  We need a mechanism which allows reads byte by byte, memory map, some control
%  message passing for controlling the driver and application and direct
%  driver-application data flow. Unlike
%  block devices which might be read only by blocks with other restrictions
%  and sockets which goes through Linux networking stack, character ones
%  satisfies all our prerequisites and so we use it. Here comes a bare
%  introduction to char subsystem internals, information about API (including
%  source code examples) are available in \cite{ldd}, chapter~3 and about how
%  it works in \cite{ulk}.

  \subsection{Device nodes}
  \label{chap.devnodes}
  We use character device approach in Liberouter already -- we have a
  separated namespace for combosix core driver and for szedata (it's an
  implementation for older firmware, discussed in chapter \ref{chap.netcope}):
  combosix* and szedata*, where * stands for any so far random number.
  
  A node in \emph{/dev} directory must be created for each such device. They
  used to be created manually by userspace programs \emph{MAKEDEV} and
  \emph{mknod}, although this was not perfect because of the need of every
  single node creation and so some dynamic principles described later in
  this section were introduced.

  The existing node might then be opened and used by userspace
  application and on the other end handled by the driver which created it.
  Which handlers exactly are necessary in implementation will be discussed in
  section \ref{chap.code} after considering operations done in specification.

  \subsection{udev}
  \label{chap.udev}
  Userspace device (udev) is a dynamic device node creation system
  with support of running external programs depending on defined configuration
  rules. It's a successor of devfs, a filesystem implemented directly in
  kernel and mounted on \emph{/dev}. Udev mounts only a temporary filesystem
  (tmpfs) on the same directory, but from its name, it does all its work in
  userspace by listening on socket for userspace events (uevents) from kernel
  and invoking mknod, modprobe\footnote{kernel module insertion program} and
  other programs.

  One particular userspace rule may look like the following one which creates
  a symbolic
  link \emph{/dev/input/event15} for Microsoft (vendor ID 0x045e) Laser Mouse
  6000 (device ID 0x00f0).
\begin{verbatim}
KERNEL=="event*", SUBSYSTEM=="input", \
        ATTRS{idVendor}=="045e",      \
        ATTRS{idProduct}=="00f0",     \
        SYMLINK="input/event15"
\end{verbatim}
  This is the point, where IDs read from PCI configuration space are sent by
  kernel in uevent and might be matched in rules file by udev.
  
  As device node names are random so far and we want numbers in names to
  correspond with each other, e.g. to have combosix6 and szedataII6 for one
  concrete card in PCI slot 6, we need to write similar rules to generate
  persistent and consistent naming.

  Two separate files are needed to achieve that, one another with persistent
  rules will be created on the fly. Shell script which augments the persistent
  rules and a rules file for matching the \com\ cards which runs the 
  script are in detail described in Appendix \ref{app.udev}.

  \section{NetCOPE hardware and szedata2 design}
  \label{chap.netcope}
  As we slightly mentioned in the introduction, all Liberouter projects are
  based on \com\ cards. Those cards contain Xilinx \emph{field-programmable
  gate arrays} (FPGA) with a PowerPC processor.
  There was an aspiration to develop an independent platform which would
  be immutable against fast evolution of networking. This is a goal of \ncope\ 
  project, currently built on the top of \com\ with network profitable
  wiring and internal connections. Technicalities may be found in report
  \cite{netcope} and, for Liberouter members, also on internal wikipedia under
  all links on \ncope\ specification page.

  The project was started in the middle of 2006 and currently we have a
  functional and tested prototype with 4 network interfaces\footnote{a new
  hardware with 2 interfaces but with modern bus (PCIe) is prepared and tested
  now}. Its universal solution allows
  involving it in multiple areas such as network traffic filtering, routing
  packets, intrusion detection and others -- it depends on what program,
  so-called \emph{design}, is loaded into the FPGA.

  \begin{figure}[htb]
    \begin{center}
      \includegraphics[width=8cm]{images/architecture}
      \caption{Overall schema of \ncope\ hardware}
      \label{fig.netcope_hw}
    \end{center}
  \end{figure}

%  \begin{figure}[htb]
%    \begin{center}
%      \includegraphics[width=11cm]{images/netcope_color}
%      \caption{NetCOPE schematic}
%      \label{fig.netcope_schema}
%    \end{center}
%  \end{figure}

  \subsection{szedata2}
  Szedata2 is one of designs, the abbreviation stands for \emph{straight
  zero copy data} in the meaning of avoid copying data from memory to the
  application and vice versa. The name was chosen after its predecessor
  szedata v. 1.

  \paragraph{Design specification}
  Both szedata versions operate in FPGA on \ncope\ architecture. Version 1
  uses PowerPC as a DMA controller -- program running on that processor used
  to transfer data through scatter-gather DMA method (described in
  \cite{ldd}, chapter~15) over memory chunk pool.
  In the first phase, the data transferred at one turn contained only one
  packet of constant length. While PowerPC was working with higher rates in
  this mode, we found out, that it's a bottleneck of the whole architecture,
  its control bus was too slow to achieve higher transfer rates.
  
  We tried to improve this behaviour by aggregating more data packets into one
  transfer block and we redefined data structure too -- data are now in packet
  of variable length with modified header in comparison to the older one.
  We had to redesign also the program running on the card a bit and define the
  packet structure in the specification. The header includes two sizes:
  segment size and header size. The former is size of whole segment, i.e.
  size of header, padding and data in sum, while the latter is size of optional
  part of segment containing some control information. This control data are
  placed directly after the header, optional padding follows and finally the
  packet contents with start aligned to 8-byte
  boundary (thanks to padding). We refer to this structure as a \emph{packet},
  its format is shown on figure \ref{fig.netcope_pkt}.

  \begin{figure}[htb]
    \begin{center}
      \includegraphics[width=7cm]{images/packet}
      \caption{szedata variable length packet}
      \label{fig.netcope_pkt}
    \end{center}
  \end{figure}

  Despite all improvements, this was still unsuitable due to grown
  complexity and still the transfers couldn't be done at estimated
  (theoretically computed) speeds.
  We decided to redesign whole design architecture and fork szedata project
  into version 2. PowerPC is no longer used for DMA transfers, it's fully
  disconnected. Four separate DMA controllers for each interface are
  implemented directly in the design instead. By that step we
  eliminated the impact of slow control bus, but we still have problems
  with data structures, the aggregation is too complex.

  After digging through some specifications such as Intel gigabit Ethernet
  cards \cite{intel_gbe}, drivers e.g. for Broadcom 43 wireless cards
  and info about projects such as Endace DAG, we considered ring buffer
  structure as an underlying structure. This was the breakpoint to start
  szedata2 Liberouter project.

  First, we needed to create a design specification. Let's consider 10\,Gb/s
  data flow and process latency of 50\,ms. We need a buffer of size
  at least $\frac{10\cdot10^{3}\cdot0,05}{8}\doteq60$\,MiB per interface not
  to throw incoming data away. But as we noted in chapter \ref{chap.mm}, we
  cannot allocate that much contiguous physical memory. This made us to split
  the buffer on the page basis and create a mechanism to cope with this.

  The idea is to allocate yet another memory for descriptor page with entries
  pointing to the pages of the interspersed ring buffer. We took in account
  64-bit systems, so that the pointers are represented by 8 bytes each. Since
  we allocate the descriptor memory on a page basis too, it allows us to have
  only $\frac{4096}{8}=512$ pointers to pages
  which gives $512\cdot4096=2$\,MiB sized ring buffer. This is not enough, so
  we introduced two kinds of descriptor entries. One is still a direct pointer
  to ring buffer pages, the other one is a pointer to the next descriptor
  table. To distinguish them, we take to advantage of all page addresses being
  zeroed in low 12~bits (see \cite{gorman}, chapter~3) -- descriptor pointers
  are logically or-ed with~1.
  
  Regs, functions\ldots

\chapter{szedata2 driver}
  \section{Development cycle}
  This thesis also aims to be a clue for other Liberouter developers, how can
  newer techniques be used in development process. We'll use some of agile
  techniques evolved in the mid-1990s and established in \emph{Agile
  Manifesto}\cite{agile}, some basic UML modeling techniques from XXX cite XXX
  and by referencing design patterns that should be used for some
  particular code parts too.

  If we abstract to simple waterfall model, code development usually starts
  after summarizing of requirements as a clue for a design creation, which is
  base for a programmer. Verification and maintenance follows. We go through
  all these steps in this section.

  \subsection{Requirements}
  All requirements were summarized and acknowledged by software group with
  hardware platform people based on the hardware specification (section
  \ref{chap.netcope}).

  XXX Huh, here we need to consider all the pros and cons of all possible
  variants.

  \subsection{Data path}
  card $\rightarrow$ host memory (kernelspace) $\rightarrow$ userspace (pcap,
  mmap)

  \paragraph{Modified libpcap}
  ref Konik's work

  \subsection{Models and design}
  To not start 

  We use for modelling \emph{unified modeling language} (UML). Several UML
  tools are
  available for Linux, we chose one named Bouml. It can do most of UML
  defined diagrams, however we'll use only use case and sequence diagrams.

  XXX citation needed, some UML resources.

  See Appendix \ref{app.models}.

  \subsubsection{Use case diagram}
  We'll start with use case diagram, because it's usually a starting point for
  other diagrams. We create a module szedata2 in which we put use cases for
  each task we will have and OS, hardware and application roles outside the
  module.

  The only OS's task is to insert and remove the module to/from system, we
  only set up this use cases which allocate or free resources needed for
  module to run. HW's only task is to inform the module that the transfer has
  been completed. This is done by timeout or position reached registers
  described already in section \ref{chap.netcope}.

  Application will have much more use cases. First, we need to initialize the
  device after opening the device node, next the application wants to get
  access to the data and need to control the device somehow and finally
  cleanup on application's close.

  All the operations must be atomic due to concurrent access, this is not
  mentioned neither in the specification nor further, because we take this for
  granted.

  \paragraph{Open and initialization} is done in the following steps. If this
  is the first open, we must allocate space for ring buffers and their
  descriptors
  and set the device to initial state including device's ring buffers setup.
  Next step is to map this memory to userspace. This is done by invokation of
  mmap by application. Now, application has all the needed information, it
  tells the driver which interfaces it wants to use and starts the device.

  \paragraph{Data processing} includes locking (asking for) data area either
  to read from or write to, do the processing and unlocking. Overall two
  method of read operation raised:
  \begin{list}{-}{}
    \item \textbf{load balancing} -- several readers exist and data are divided
      among them. They all get distinct data and do the same operation on
      them. It's very similar to \emph{single program multiple data} in
      processors theory.
    \item \textbf{multiprocessing} -- all readers want all data to do
      different tasks with them. We can approach this to \emph{multiple
      program single data}.
  \end{list}
  We implement only the latter so far, but it's possible that we augment the
  implementation to the former too, see \ref{chap.future}.

  For outgoing direction, there is only one possibility which hardware permits.
  Only one writer at a time is allowed, we take this in account even
  while designing the implementation. There is a plan to extend this behaviour
  to allow multiple writers too, it has not been finished.

  \paragraph{Close and deinitialization} is done only after all application
  have been already closed. After such last close we first stop the device
  and then free all the allocated resources and mappings.

  \subsubsection{Sequence diagram}
  These are usually used for depicting how events happen during some time
  period. The topmost event is done first and then all events below it
  towards the bottom one. XXX seqd citation needed XXX

  We create 2 sequence diagrams. One for receiving and one for sending data.
  They are slightly different in how application locks and unlocks data.

  \paragraph{RX sequence diagram} starts when open is called by an
  application. We need to allocate descriptors for ring buffer, the ring
  buffer itself if we didn't do it on module insertion (this is the first
  optional part in the diagram). Next we \emph{mmap} a page with status
  information
  and space with ring buffers, choose which interfaces the application wants
  to read from and emits start control message which results in start and end
  pointer registers setup if it wasn't already done by some previous and still
  running application and passing start value to control register.

  A loop follows in which the application periodically calls lock, processes
  data if any available and unlocks the area. After it finishes its work
  (interrupts the loop), it calls stop which in return stops the device if it
  was the last user, invokes \emph{unmap} on all earlier mapped
  resources and closes the device node. If we allocate the ring buffer in the
  open, we will free it now and return back to the userspace, where the caller
  might exit. When the application ends unexpectedly (e.g. crashes), we must
  do the cleanup work too. The convenience of the OS is that it will unmap
  all spaces
  call the close itself, so that we see this the same. The only exception is
  that any control message won't be sent in such case, so we must check in
  close if the device is stopped already or not.

  \paragraph{TX sequence diagram} differs only in processing between lock and
  unlock phase.
  It gets locked area of some bytes, which it must fill, not process and there
  might be only one application which might have locked area at a time.

  \subsection{Code development}
  \label{chap.code}
  We finally can start with the implementation of the specification showed
  above.
  
%  \subsubsection{Code templates}
%  + design patterns if any applies

  \subsubsection{Combo6 code base}
  We touched this area slightly in section \ref{chap.devnodes} already, but
  make these things clear. All Liberouter modules are based on combo6core and
  one of combo6 and combo6x modules. It's transparent to them which one of
  them is in use, because the core encapsulates the behaviour -- there is
  defined API in the core sources which might be used. They initialize the
  base card, remap all needed spaces common to all base and additional cards
  and export any useful info in proc and sysfs XXX citation needed XXX file
  systems.

  combo6x driver as a base, new szedata2 module

  \subsubsection{libsze2}
  Although there is no library for szedata v. 1, we consider to have one for
  szedata2. The main point to have it is not to propagate the kernel
  interface directly to the user, to simplify the interface and make it
  abstract.
  
  The library functions might be an exact copy of use cases from the
  specification, since the use case alone means exactly that it is a
  separate sequence of commands which might not be further divided. If we
  encapsulate each use case's contents in a library call, we reach the
  aims from previous paragraph. Above all, if we create a shared library,
  we'll get easy to update part (module) of each application.

  \subsubsection{Mmap space structure}
  \label{chap.mmap}
  locked : desc : rx ring : tx ring

  \label{chap.hwsetup}

  \subsubsection{Back-porting to older kernels}
  Why we should do this? Macros used (irq handler vs. 2.6.19, mmap vs.
  remap\_pfn\_range, remap\_page\_range), workarounds (class+class\_simple
  vs. 2.6.16).

  \subsubsection{Final code}
  Nice, working, debugged kernel module as a result. Documentation.
  License (\cite{gplv2}).

  \section{Verification}
  Testing (TDD) and tools (sparse).

  \section{Maintenance}

\chapter{Future work}
  \label{chap.future}
  DMA DAC, standard network adapter mode (kernel network stack usage). SPMD

\chapter{Conclusion}
  XXX\cite{ulk}

\bibliographystyle{plain} 
\bibliography{dp}

\include{app}

\end{document}
