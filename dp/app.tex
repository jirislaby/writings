% vim: textwidth=78 ai

\appendix
  \addtocontents{toc}{\setcounter{tocdepth}{2}}
  \chapter{Udev files}
  \label{app.udev}
  Commented file listings of udev rules and a script which is called from
  those rules follow. This package was developed and tested on udev 120.

  \section{udev rules file}
\begin{lstlisting}[language={},label=lst.udevrules,name=lst.udevrules]
KERNEL!="combosix*", GOTO="cesnet_generator_end"(*@\label{lstl.udevrules_1}@*)
ACTION!="add", GOTO="cesnet_generator_end"(*@\label{lstl.udevrules_2}@*)
NAME=="?*", GOTO="cesnet_generator_end"(*@\label{lstl.udevrules_3}@*)
\end{lstlisting}
  Since we want to match only additions of combosix devices, rules at line
  \ref{lstl.udevrules_1} and \ref{lstl.udevrules_2} are needed. To ignore
  cases where the stored name was set yet, the skip rule at line
  \ref{lstl.udevrules_3} is there.
\begin{lstlisting}[name=lst.udevrules]
ENV{CS}="$kernel"
ENV{MATCHPATH}="$devpath"
\end{lstlisting}
  Set up environment variables used in a shell script later. These variables are
  filled in directly by \emph{udev} and corresponds to the name suggested by
  the kernel or a device path (i.e. bus, slot\ldots) respectively.
\begin{lstlisting}[name=lst.udevrules]
IMPORT{PROGRAM}="csdevname" (*@\label{lstl.udevrules_11}@*)

ENV{CS_NEW}=="?*", NAME="$env{CS_NEW}"

LABEL="cesnet_generator_end" (*@\label{lstl.udevrules_12}@*)
\end{lstlisting}
  And finally invoke the script and set up the new name if the script changed
  it. The last step (line \ref{lstl.udevrules_12}) is to define a label where
  the first 3 rules jump to.

  \section{csdevname script}
  Shell script \textbf{csdevname} called from line \ref{lstl.udevrules_11}
  above follows:
\lstset{language=bash} % XXX TODO
\begin{lstlisting}[label=lst.udevscript,name=lst.udevscript]
RULES_FILE='/etc/udev/rules.d/70-cesnet.rules'

. /lib/udev/rule_generator.functions(*@\label{lstl.udevscript_1}@*)
\end{lstlisting}
  This loads udev predefined functions used later. RULES\_FILE is the
  name of file, where we store our persistent naming. Variable name is
  predefined (and used in the script sourced at line \ref{lstl.udevscript_1})
  and must not be changed.
\begin{lstlisting}[name=lst.udevscript]
cs_name_taken() {
	local value="$(find_all_rules 'NAME=' $CS)"
	if [ "$value" ]; then return 0; else return 1; fi
}

find_next_available() {
	raw_find_next_available "$(find_all_rules 'NAME=' "$1")"
}
\end{lstlisting}
  Functions \textbf{cs\_name\_taken} and \textbf{find\_next\_available} are
  for finding available names for new coming device. Both uses functions
  defined in the sourced script from line \ref{lstl.udevscript_1}, that allows
  be them as compact as possible.
\begin{lstlisting}[name=lst.udevscript]
write_rule() {
	local match="$1"
	local name="$2"
	local comment="$3"
	{
		[ "$comment" ] && echo "# $comment"
		echo "SUBSYSTEM==\"combosix\", ACTION==\"add\"$match, NAME=\"$name\""
	} >> $RULES_FILE
}
\end{lstlisting}
  \textbf{write\_rule} takes 3 parameters -- under which condition this rule
  is triggered, static node name and potentially a comment to this rule. The
  result is written to previously defined \textbf{RULES\_FILE}.
\begin{lstlisting}[name=lst.udevscript]
lock_rules_file(*@\label{lstl.udevscript_27}@*)
choose_rules_file(*@\label{lstl.udevscript_29}@*)
\end{lstlisting}
  At lines \ref{lstl.udevscript_27} and \ref{lstl.udevscript_29} we lock up
  the rules file to prevent race
  conditions (if two writers occurred, it would result in unpredictable
  behavior) and decide in which file will be the new rules written (since
  an usual location might be still read-only while booting -- \emph{udev} will
  then move the temporary file to the proper directory after it becomes
  writable).
\begin{lstlisting}[name=lst.udevscript]
MATCHPATH=${MATCHPATH%/*/*}
if [ "$MATCHPATH" ]; then
	match="$match, DEVPATH==\"$MATCHPATH/*\""
fi

if [ -z "$match" ]; then
	echo "missing valid match" >&2
	unlock_rules_file
	exit 1
fi
\end{lstlisting}
  Now we define matching rule passed to \textbf{write\_rule} later (line
  \ref{lstl.udevscript_49}). Sanity check of the value is done here and if it
  fails we return error.
\begin{lstlisting}[name=lst.udevscript]
if [ "$CS_NAME" ]; then
	COMMENT="$COMMENT (custom name)"
	if [ "$CS_NAME" != "$CS" ]; then
		CS=$CS_NAME;
		echo "CS_NEW=$CS"
	fi
else
	base=${CS%%[0-9]*}
	if cs_name_taken; then
		CS="$base$(find_next_available "$base[0-9]*")"
		echo "CS_NEW=$CS"
	fi
fi
\end{lstlisting}
  Here the name, which is returned to rules (\textbf{CS\_NEW}), is set. Logic
  is simple, either the one passed by external application is used or a new
  one is found (first unused).
\begin{lstlisting}[name=lst.udevscript]
write_rule "$match" "$CS" "$COMMENT"(*@\label{lstl.udevscript_49}@*)

unlock_rules_file
\end{lstlisting}
  Finally the file with static naming is augmented and unlocked.

  Since this moment, if the device come in the system again later, even in
  other order, system will ever create a node with the same name loaded from
  the rules file.
\chapter{Models}
  \label{app.models}
  Models created using \emph{bouml} are contained in this appendix. First we
  include the use case diagram with specification of its parts directly after
  it.
  \begin{center}
    \includegraphics[width=12.5cm]{images/usecase}
  \end{center}

  \section{Use Case open\_node}
  \begin{CompactEnumerate}
    \item the module's device node is opened
    \item per application descriptor allocation is done including space for
      a status structure
    \item if (not allocate\_on\_modprobe and refcount was 0) then call
      \emph{alloc\_rings}
    \item node is mmap-ed (offset 0) -- status
    \item node is mmap-ed with values from previously mapped memory (rx, tx
      spaces) -- note that all buffers are visible as a virtual contiguos
      memory. The only difference between TX and RX mmio space is read/write
      protection
  \end{CompactEnumerate}

  \section{Use Case subscribe}
  \subsection{Precondition}
    Node must be opened yet and close must not to be in progress

  \subsection{Normal flow}
  \begin{CompactEnumerate}
    \item subscribe\_area ioctl is called with areas bitmap and poll\_thresh;
      both for each direction
    \item APP poll\_thresh and areas are set to the status page
  \end{CompactEnumerate}

  \section{Use Case start}
  \subsection{Precondition}
    Node must be opened yet and close must not to be in progress

  \subsection{Normal flow}
  \begin{CompactEnumerate}
    \item start ioctl is called
    \item each APP rx\_tail\_ptr and APP rx\_head\_ptr is set to the current
      corresponding HW rx\_tail\_ptr
    \item if (refcount was 0) then the device is started -- enabling of
      interrupts, capture start ...
  \end{CompactEnumerate}

  \section{Use Case wait\_for\_data}

  \subsection{Precondition}
  Node must be opened yet and close must not to be in progress

  \subsection{Normal flow}
  \begin{CompactEnumerate}
    \item poll is invoked
    \item while:
    \begin{CompactEnumerate}
      \item for all requested rx areas: difference(HW rx\_head\_ptr, APP
        rx\_tail\_ptr) $<$ APP rx\_poll\_thresh and
      \item for all requested tx areas: (tx\_ring\_size - difference(HW
        tx\_head\_ptr, HW tx\_tail\_ptr)) $<$ APP tx\_poll\_thresh
    \end{CompactEnumerate}
      do: the proccess is slept and interrupt scheduled
    \item return status to the application
  \end{CompactEnumerate}

  \subsection{Alternative flow}
  \begin{CompactItemize}
    \item signal is catched while sleeping $\rightarrow$ interrupt waiting and
      return
  \end{CompactItemize}

  \section{Use Case get\_data}
  \subsection{Precondition}
  Node must be opened yet and close must not to be in progress

  \subsection{Normal flow}
  \begin{CompactEnumerate}
    \item rx\_lock\_data ioctl is invoked with requested areas bitmap
    \item the requested bitmap is logically ANDed with the subscribed bitmap
    \item size is computed as difference (APP rx\_tail\_ptr, HW rx\_head\_ptr)
      for each area until size is zero and this area number is stored
    \item the size (even 0 is valid) is stored into the status page along with
      offset obtained from APP rx\_tail\_ptr
    \item offset + size sum is stored into the right APP rx\_head\_ptr
    \item stored area is returned
  \end{CompactEnumerate}

  \subsection{Alternative flow}
  \begin{CompactItemize}
    \item size is zero -- invalid area is returned
  \end{CompactItemize}

  \section{Use Case release\_data}

  \subsection{Precondition}
  Node must be opened yet and close must not to be in progress

  \subsection{Normal flow}
  \begin{CompactEnumerate}
    \item rx\_unlock\_data ioctl was invoked
    \item foreach area do
    \begin{CompactEnumerate}
      \item corresponding APP rx\_tail\_ptr is set to the APP rx\_head\_ptr
        value previously stored in get\_data
      \item HW rx\_tail\_ptr is updated to the min(APP rx\_tail\_ptrs $>$ HW
        rx\_tail\_ptr), if there is no such value, then use min(APP
	rx\_tail\_ptrs) of corresponding area pointers. This is needed to
	catch pointers actually higher than the others, but still belonging to
	the slowest readers (head usually wraps around and continues from the
	entry number 0).
    \end{CompactEnumerate}
  \end{CompactEnumerate}

  \section{Use Case request\_space}

  \subsection{Precondition}
  Node must be opened yet and close must not to be in progress.
  Application must have this area subscribed for tx.
  TX for requested area is not in progress (only one writer at a~time).

  \subsection{Normal flow}
  \begin{CompactEnumerate}
    \item request\_space ioctl is called with size and area parameters
    \item size is computed as min(max\_tx\_request\_count, requested size,
      tx\_ring\_buffer\_size - difference(HW tx\_head\_ptr, HW tx\_tail ptr))
    \item the size and HW tx\_head\_ptr as offset is stored into APP
      status\_page
  \end{CompactEnumerate}

  \section{Use Case put\_data}

  \subsection{Precondition}
  Node must be opened yet and close must not to be in progress.
  request\_space was called by this application for area in parameter.

  \subsection{Normal flow}
  \begin{CompactEnumerate}
    \item put\_data ioctl is invoked with filled size and area where the data was
  filled to
    \item min(size computed in request\_space, supplied size) is added to the
  HW.tx\_head\_ptr
  \end{CompactEnumerate}

  \section{Use Case close\_node}

  \subsection{Precondition}
  open\_node was invoked before this call

  \subsection{Normal flow}
  \begin{CompactEnumerate}
    \item stop ioctl was called
    \item node is unmap-ed
    \item module's close function is called
    \item if (refcount will be 0)
  a) the device is stopped (it's waited for dma transfers to be
  aborted/finished)
  b) if (allocation on modprobe) then null HW ptrs else call dealloc\_rings
    \item per application descriptor is deallocated
  \end{CompactEnumerate}


  \section{Use Case interrupt}

  \subsection{Precondition}
  Hardware was started and expected to run (i.e. refcount $>$ 0)

  \subsection{Normal flow}
  \begin{CompactEnumerate}
    \item interrupt (timeout or ring buffer pointer reached) is raised -- ring
  buffer is in state we requested
    \item wake applications, which are waiting for this info up
  \end{CompactEnumerate}

  \subsection{Alternative flow}
  \begin{CompactItemize}
    \item missed interrupt -- set up timer (watchdog) to cope with this
  \end{CompactItemize}

  \section{Use Case insert\_module}
  \begin{CompactEnumerate}
    \item modprobe/insmod the module is invoked
    \item if (allocate on modpobe) then call alloc\_rings
  \end{CompactEnumerate}

  Note: 'allocate on modpobe' is set to true by default, but might be
  overriden by module parameter at insertion time

  \section{Use Case remove\_module}

  \begin{CompactEnumerate}
    \item rmmod the module is called
    \item if (allocate on modpobe) then call dealloc\_rings
  \end{CompactEnumerate}


  \section{Use Case alloc\_rings}

  \begin{CompactEnumerate}
    \item alloc\_ring is called
    \item pages for the ring buffer are allocated, each interface has its own
      area in the buffer (each interface has its own DMA controller and a
      ring buffer)
    \item physical addresses are put to the hardware page pointers list -- the
      last descriptor points to the first
  \end{CompactEnumerate}

  \section{Use Case dealloc\_rings}

  \begin{CompactEnumerate}
    \item all ring buffer (DMA) mappings and buffers are unmapped and
      deallocated
  \end{CompactEnumerate}

  \section{Sequence Diagrams}
  Now we include sequence diagrams, as the first we depict RX diagram followed
  by TX one. Remember section~\ref{chap.spec}, where we described how we
  constructed them.

  \newpage
  \begin{center}
    \includegraphics[width=15cm]{images/seqd_rx}
  \end{center}

  \newpage
  \begin{center}
    \includegraphics[width=15cm]{images/seqd_tx}
  \end{center}


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\chapter{Doxygen documentation}
  \label{app.doxygen}
  In this appendix we include generated doxygen documention from the userspace
  library. All mentioned files are available to see on enclosed CD.
  \section{Class Documentation}
  \input{doc/structszedata2__packet}
  \input{doc/structszedata__lock}
  \section{File Documentation}
  \input{doc/libsze2_8h}

%\setlength{\parskip}{-0.4em}

% s/description/kdocdescription/
% s/subsection/kdocsubsection/
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\chapter{Kernel-doc documentation}
  \label{app.kdoc}
% ------------------------   
% Section 
\section{Userspace interface}
\label{id230790}\hypertarget{id230790}{}%
% 
% -------------------------------------------------------------
% Refentry 
% ------------------------------------------------------------- 	
\section*{struct sze2\_instance\_info}
\label{API-struct-sze2-instance-info}\hypertarget{API-struct-sze2-instance-info}{}%
\label{id236309}

\section*{Name}
struct sze2\_instance\_info --- info mapped at offset 0\label{id207141}
\kdocsubsection*{Synopsis}

\begin{Verbatim}[]
struct sze2_instance_info {
  __u32 magic;
  __u32 reserved1;
  __u64 offsets[SZE2_MMIO_MAX];
  __u64 sizes[SZE2_MMIO_MAX];
  __u32 areas[2];
  __u64 reserved2[5];
  struct sze2_adesc adesc[0][2];
};  
\end{Verbatim}

\kdocsubsection*{Members}
\label{id207154}

\begin{kdocdescription}
% \null and \mbox are tricks to induce different typesetting decisions
\item[{magic}]
0xde4dAAII (mAjor=0x02, mInor)
% \null and \mbox are tricks to induce different typesetting decisions
\item[{reserved1}]
for future/testing purposes
% \null and \mbox are tricks to induce different typesetting decisions
\item[{offsets[SZE2\_MMIO\_MAX]}]
offset of each mmap space
% \null and \mbox are tricks to induce different typesetting decisions
\item[{sizes[SZE2\_MMIO\_MAX]}]
size of each mmap space
% \null and \mbox are tricks to induce different typesetting decisions
\item[{areas[2]}]
subscribed areas (rx and tx)
% \null and \mbox are tricks to induce different typesetting decisions
\item[{reserved2[5]}]
reserved for future purposes
% \null and \mbox are tricks to induce different typesetting decisions
\item[{adesc[0][2]}]
desc for each area and direction
\end{kdocdescription}
\noindent % 
% -------------------------------------------------------------
% Refentry 
% ------------------------------------------------------------- 	
\section*{struct sze2\_subscribe\_area}
\label{API-struct-sze2-subscribe-area}\hypertarget{API-struct-sze2-subscribe-area}{}%
\label{id206526}

\section*{Name}
struct sze2\_subscribe\_area --- SUBSCRIBE\_AREA ioctl parameter\label{id206536}
\kdocsubsection*{Synopsis}

\begin{Verbatim}[]
struct sze2_subscribe_area {
  __u32 areas[2];
  __u32 poll_thresh[2];
};  
\end{Verbatim}

\kdocsubsection*{Members}
\label{id206548}

\begin{kdocdescription}
% \null and \mbox are tricks to induce different typesetting decisions
\item[{areas[2]}]
bitmap of areas to be subscribed
% \null and \mbox are tricks to induce different typesetting decisions
\item[{poll\_thresh[2]}]
minimal size which will wake the application from poll sleep
\end{kdocdescription}
\noindent 
\kdocsubsection*{Description}
\label{id206580}

if areas are set to \textasciitilde{}0U, it means {``}all available{''}, corrected value is in mmap space after the ioctl
% 
% -------------------------------------------------------------
% Refentry 
% ------------------------------------------------------------- 	
\section*{struct sze2\_tx\_lock}
\label{API-struct-sze2-tx-lock}\hypertarget{API-struct-sze2-tx-lock}{}%
\label{id206682}

\section*{Name}
struct sze2\_tx\_lock --- TXLOCKDATA ioctl parameter\label{id206692}
\kdocsubsection*{Synopsis}

\begin{Verbatim}[]
struct sze2_tx_lock {
  __u32 size;
  __u8 area;
};  
\end{Verbatim}

\kdocsubsection*{Members}
\label{id206704}

\begin{kdocdescription}
% \null and \mbox are tricks to induce different typesetting decisions
\item[{size}]
how many bytes should be reserved
% \null and \mbox are tricks to induce different typesetting decisions
\item[{area}]
which area to involve
\end{kdocdescription}
\noindent % 
% -------------------------------------------------------------
% Refentry 
% ------------------------------------------------------------- 	
\section*{struct sze2\_tx\_unlock}
\label{API-struct-sze2-tx-unlock}\hypertarget{API-struct-sze2-tx-unlock}{}%
\label{id206775}

\section*{Name}
struct sze2\_tx\_unlock --- TXUNLOCKDATA ioctl parameter\label{id206785}
\kdocsubsection*{Synopsis}

\begin{Verbatim}[]
struct sze2_tx_unlock {
  __u32 size;
  __u8 area;
};  
\end{Verbatim}

\kdocsubsection*{Members}
\label{id206797}

\begin{kdocdescription}
% \null and \mbox are tricks to induce different typesetting decisions
\item[{size}]
how many bytes were really written
% \null and \mbox are tricks to induce different typesetting decisions
\item[{area}]
to which area; it must be locked earlier
\end{kdocdescription}
\noindent 
% ------------------------   
% Section 
\section{Intradriver interface}
\label{id244787}\hypertarget{id244787}{}%
% 
% -------------------------------------------------------------
% Refentry 
% ------------------------------------------------------------- 	
\section*{struct szedata2\_ring}
\label{API-struct-szedata2-ring}\hypertarget{API-struct-szedata2-ring}{}%
\label{id244829}

\section*{Name}
struct szedata2\_ring --- szedata per ring buffer info\label{id244839}
\kdocsubsection*{Synopsis}

\begin{Verbatim}[]
struct szedata2_ring {
  unsigned int blks;
  unsigned int areas;
  size_t size;
  struct szedata2_block * ring;
};  
\end{Verbatim}

\kdocsubsection*{Members}
\label{id244852}

\begin{kdocdescription}
% \null and \mbox are tricks to induce different typesetting decisions
\item[{blks}]
blocks per area
% \null and \mbox are tricks to induce different typesetting decisions
\item[{areas}]
areas per space
% \null and \mbox are tricks to induce different typesetting decisions
\item[{size}]
block size
% \null and \mbox are tricks to induce different typesetting decisions
\item[{ring}]
pointers to ring blocks
\end{kdocdescription}
\noindent 
% ------------------------   
% Section 
\section{Public functions}
\label{id244906}\hypertarget{id244906}{}%
% 
% -------------------------------------------------------------
% Refentry 
% ------------------------------------------------------------- 	
\section*{szedata2\_alloc\_dmaspace}
\label{API-szedata2-alloc-dmaspace}\hypertarget{API-szedata2-alloc-dmaspace}{}%
\label{id244948}

\section*{Name}
szedata2\_alloc\_dmaspace --- allocate DMAable ring buffers\label{id244958}
\kdocsubsection*{Synopsis}
\label{id244963}int \textbf{ szedata2\_alloc\_dmaspace } ({\ttfamily\itshape{{sd}}}, {\ttfamily\itshape{{space}}}, {\ttfamily\itshape{{areas}}}, {\ttfamily\itshape{{count}}}, {\ttfamily\itshape{{size}}});
	\newline
	struct szedata2 * {\ttfamily\itshape{{sd}}};
	\newline
	unsigned int {\ttfamily\itshape{{space}}};
	\newline
	unsigned int {\ttfamily\itshape{{areas}}};
	\newline
	unsigned int {\ttfamily\itshape{{count}}};
	\newline
	size\_t {\ttfamily\itshape{{size}}};
\kdocsubsection*{Arguments}
\label{id245013}

\begin{kdocdescription}
% \null and \mbox are tricks to induce different typesetting decisions
\item[{{\ttfamily\itshape{{sd}}}}]
szedata2 structure
% \null and \mbox are tricks to induce different typesetting decisions
\item[{{\ttfamily\itshape{{space}}}}]
RX/TX
% \null and \mbox are tricks to induce different typesetting decisions
\item[{{\ttfamily\itshape{{areas}}}}]
number of ring buffers to allocate
% \null and \mbox are tricks to induce different typesetting decisions
\item[{{\ttfamily\itshape{{count}}}}]
number of block count
% \null and \mbox are tricks to induce different typesetting decisions
\item[{{\ttfamily\itshape{{size}}}}]
block size (must be PAGE\_SIZE aligned)
\end{kdocdescription}
\noindent 
\kdocsubsection*{Description}
\label{id245102}

Parent device must be set (on alloc\_szedata2) and reference to it is raised by one here.
% 
% -------------------------------------------------------------
% Refentry 
% ------------------------------------------------------------- 	
\section*{szedata2\_free\_dmaspace}
\label{API-szedata2-free-dmaspace}\hypertarget{API-szedata2-free-dmaspace}{}%
\label{id245148}

\section*{Name}
szedata2\_free\_dmaspace --- free DMAable space\label{id245158}
\kdocsubsection*{Synopsis}
\label{id245163}void \textbf{ szedata2\_free\_dmaspace } ({\ttfamily\itshape{{sd}}}, {\ttfamily\itshape{{space}}});
	\newline
	struct szedata2 * {\ttfamily\itshape{{sd}}};
	\newline
	unsigned int {\ttfamily\itshape{{space}}};
\kdocsubsection*{Arguments}
\label{id245190}

\begin{kdocdescription}
% \null and \mbox are tricks to induce different typesetting decisions
\item[{{\ttfamily\itshape{{sd}}}}]
szedata2 structure
% \null and \mbox are tricks to induce different typesetting decisions
\item[{{\ttfamily\itshape{{space}}}}]
RX/TX
\end{kdocdescription}
\noindent % 
% -------------------------------------------------------------
% Refentry 
% ------------------------------------------------------------- 	
\section*{szedata2\_get\_addr}
\label{API-szedata2-get-addr}\hypertarget{API-szedata2-get-addr}{}%
\label{id245268}

\section*{Name}
szedata2\_get\_addr --- get virtual and physical address of ring buffer\label{id245277}
\kdocsubsection*{Synopsis}
\label{id245282}size\_t \textbf{ szedata2\_get\_addr } ({\ttfamily\itshape{{sd}}}, {\ttfamily\itshape{{space}}}, {\ttfamily\itshape{{area}}}, {\ttfamily\itshape{{virt}}}, {\ttfamily\itshape{{phys}}}, {\ttfamily\itshape{{pos}}}, {\ttfamily\itshape{{len}}});
	\newline
	struct szedata2 * {\ttfamily\itshape{{sd}}};
	\newline
	unsigned int {\ttfamily\itshape{{space}}};
	\newline
	unsigned int {\ttfamily\itshape{{area}}};
	\newline
	void ** {\ttfamily\itshape{{virt}}};
	\newline
	dma\_addr\_t * {\ttfamily\itshape{{phys}}};
	\newline
	unsigned long {\ttfamily\itshape{{pos}}};
	\newline
	size\_t {\ttfamily\itshape{{len}}};
\kdocsubsection*{Arguments}
\label{id245346}

\begin{kdocdescription}
% \null and \mbox are tricks to induce different typesetting decisions
\item[{{\ttfamily\itshape{{sd}}}}]
szedata2 structure
% \null and \mbox are tricks to induce different typesetting decisions
\item[{{\ttfamily\itshape{{space}}}}]
RX/TX
% \null and \mbox are tricks to induce different typesetting decisions
\item[{{\ttfamily\itshape{{area}}}}]
ring buffer index
% \null and \mbox are tricks to induce different typesetting decisions
\item[{{\ttfamily\itshape{{virt}}}}]
pointer to where to store the demanded virtual address
% \null and \mbox are tricks to induce different typesetting decisions
\item[{{\ttfamily\itshape{{phys}}}}]
pointer to where to store the demanded physical address
% \null and \mbox are tricks to induce different typesetting decisions
\item[{{\ttfamily\itshape{{pos}}}}]
offset of demanded position
% \null and \mbox are tricks to induce different typesetting decisions
\item[{{\ttfamily\itshape{{len}}}}]
demanded length
\end{kdocdescription}
\noindent 
\kdocsubsection*{Description}
\label{id245467}

Length of space to the next page boundary is returned (if pos is \&-ed with PAGE\_MASK, then the returned length should be PAGE\_SIZE).
% 
% -------------------------------------------------------------
% Refentry 
% ------------------------------------------------------------- 	
\section*{alloc\_szedata2}
\label{API-alloc-szedata2}\hypertarget{API-alloc-szedata2}{}%
\label{id245514}

\section*{Name}
alloc\_szedata2 --- allocate new szedata2 structure\label{id245524}
\kdocsubsection*{Synopsis}
\label{id245529}struct szedata2 * \textbf{ alloc\_szedata2 } ({\ttfamily\itshape{{private\_size}}}, {\ttfamily\itshape{{parent}}});
	\newline
	unsigned int {\ttfamily\itshape{{private\_size}}};
	\newline
	struct device * {\ttfamily\itshape{{parent}}};
\kdocsubsection*{Arguments}
\label{id245556}

\begin{kdocdescription}
% \null and \mbox are tricks to induce different typesetting decisions
\item[{{\ttfamily\itshape{{private\_size}}}}]
extra size to allocate; pointer to it is set to .private
% \null and \mbox are tricks to induce different typesetting decisions
\item[{{\ttfamily\itshape{{parent}}}}]
device which this device is bound to
\end{kdocdescription}
\noindent 
\kdocsubsection*{Description}
\label{id245598}

You usually want to call this when new device comes. Then you pass this to register after you allocate some spaces and further initialization.

Parent is needed when you plan to allocate DMA space through this module.

Pointer to new device is returned, or ERR\_PTR encoded retval, respectively.
% 
% -------------------------------------------------------------
% Refentry 
% ------------------------------------------------------------- 	
\section*{szedata2\_register}
\label{API-szedata2-register}\hypertarget{API-szedata2-register}{}%
\label{id245649}

\section*{Name}
szedata2\_register --- registerr allocate szedata2 device\label{id245658}
\kdocsubsection*{Synopsis}
\label{id245664}int \textbf{ szedata2\_register } ({\ttfamily\itshape{{sd}}});
	\newline
	struct szedata2 * {\ttfamily\itshape{{sd}}};
\kdocsubsection*{Arguments}
\label{id245684}

\begin{kdocdescription}
% \null and \mbox are tricks to induce different typesetting decisions
\item[{{\ttfamily\itshape{{sd}}}}]
structure allocated by alloc\_szedata2
\end{kdocdescription}
\noindent 
\kdocsubsection*{Description}
\label{id245709}

Call this after you setup (owner, hooks, ...) the structure and, if you plan to allocate DMA now, after you do so (on open alternatively).

Returns 0 on succes, otherwise -errno.
% 
% -------------------------------------------------------------
% Refentry 
% ------------------------------------------------------------- 	
\section*{szedata2\_destroy}
\label{API-szedata2-destroy}\hypertarget{API-szedata2-destroy}{}%
\label{id245758}

\section*{Name}
szedata2\_destroy --- cleanup szedata2 structure\label{id245768}
\kdocsubsection*{Synopsis}
\label{id245773}void \textbf{ szedata2\_destroy } ({\ttfamily\itshape{{sd}}});
	\newline
	struct szedata2 * {\ttfamily\itshape{{sd}}};
\kdocsubsection*{Arguments}
\label{id245793}

\begin{kdocdescription}
% \null and \mbox are tricks to induce different typesetting decisions
\item[{{\ttfamily\itshape{{sd}}}}]
structure to cleanup
\end{kdocdescription}
\noindent 
\kdocsubsection*{Description}
\label{id245818}

It's legal to destroy both registered and unregister structure.

\chapter{CD Contents And Usage}
  CD contents is enumerated in these points:
  \begin{CompactItemize}
    \item linux-drivers -- drivers including \sze
    \item software -- software branch with libsze2 library and szetest2
    \item sze2 -- model by \emph{bouml}
    \item sze2-generated -- HTML generated model
    \item doxygen -- HTML generated \emph{doxygen} documentation
    \item kernel-doc -- HTML generated \emph{kernel-doc} documentation
    \item README -- short information about the CD contents
    \item LICENSE -- GPL v. 2 license
  \end{CompactItemize}

  All linux drivers can be compiled by a \emph{./svncompile} script and
  installed by \emph{make install}, both from \emph{linux-drivers} directory.
  \sze\ driver
  is in \emph{linux-drivers/kernel/drivers/szedata2/} and \emph{szetest} dummy
  module used for testing in the phase~I development (see
  section~\ref{chap.code}) in \emph{linux-drivers/build/drivers/szedata2/}.

  Installed drivers may be loaded with standard \emph{modprobe}, e.g.
  \emph{modprobe szetest2} will load the driver with all its dependences.

  When the driver is ready, we can try the functionality with \emph{szetest2}
  program. It is in the \emph{software/trunk/tools/development/szetest2/}
  directory, but we first need to compile the library from
  \emph{software/trunk/libs/libsze2/}. In
  that place, we run \emph{make} to create a \emph{libsze2.so} object and then
  we can also run \emph{make} from the \emph{szetest2} directory which will
  build and link against the library.

  Invocation of the test utility must be preceded by setting up proper
  library path to tell the operating system (the linker) where to find
  libraries. This is performed by \emph{export
  LD\_LIBRARY\_PATH=$<\ldots>$/software/trunk/libs/libsze2/} (see dynamic
  linker manual page: \emph{man ld}). Then we are able to run the test:
  \vspace{.5em}
\begin{verbatim}
$ ./szetest
sub: 1 0
szetest2: poll error
sub1: 0 1
\end{verbatim}
  \vspace{.5em}

  It outputs an error, but it is one of our test cases, so this is fine. Two
  \emph{sub} lines denotes subscription first of RX direction, then TX. When
  we send some packets, it will output a character every 10\,000 received and
  sent packets and also per second statistics.

  The doxygen documentation may be generated in the \emph{doc} subdirectory of
  library directory by \emph{make}. The kernel one by
  \emph{scripts/kernel-doc}, see
  \emph{Documentation/kernel-doc-nano-HOWTO.txt} to see how this can be
  achieved. Both the script and the documentation are available in the kernel
  sources (\url{http://kernel.org/}).
