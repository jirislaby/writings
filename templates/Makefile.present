TEX=latex
PROJ=present
ifeq ("$(NH)", "")
	MOREOPTS = ",handout"
endif

all: pdf

-include Makefile.local

pdf: $(PROJ).pdf
ps: $(PROJ).ps

$(PROJ).dvi: $(PROJ).tex FORCE

$(PROJ).tex: class.tex

class.tex: FORCE
	@echo "\documentclass[xcolor=dvipsnames$(MOREOPTS)]{beamer}" > class.tex

%.pdf: %.tex FORCE
	pdflatex $<

%.ps: %.tex FORCE
	pslatex $<
	dvips $(subst .tex$,.dvi,$<) -o$@
	rm $(subst .tex$,.dvi,$<)

clean:
	rm -f $(PROJ).{pdf,dvi,toc,aux,log,bbl,blg,ps,out,nav,snm,vrb} missfont.log

FORCE:

.PHONY: FORCE all pdf ps clean
